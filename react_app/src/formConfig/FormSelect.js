import React from 'react';

const FormSelect = (props) => {
    return (
        <div className="form_element">
            <select>

            </select>
        </div>
    );
};

export default FormSelect;
